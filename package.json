{
  "name": "poboxbackend",
  "private": true,
  "scripts": {
    "start:test": "cp .env.test backend/.env && npm --prefix backend start",
    "start:live": "cp .env.live backend/.env && npm --prefix backend start",
    "test": "jest",
    "postinstall": "npm install --prefix backend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^8.5.2",
    "firebase-admin": "^13.0.2",
    "helmet": "^8.2.0",
    "stripe": "^17.3.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.13.0",
    "jest": "^29.7.0",
    "supertest": "^7.2.2"
  }
}
